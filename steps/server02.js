// Declare a variable called `PORT` and set it equal to 3000.
// Then chain the `listen` method to `app` and pass it `PORT` as first argument

const express = require('express');
const app = express();
